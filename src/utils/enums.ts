export enum CollectionNames {
  Suppliers = 'suppliers',
  Projects = 'projects',
  Audit = 'audit',
}

export enum Database {
  dbName = 'DDG_STAGE',
}

export enum Operation {
  Create = 'CREATE',
  Update = 'UPDATE',
  Delete = 'DELETE',
}
